Apply from the project root:

  cd ~/Downloads/OI/Xadrez
  python3 apply_xadrez_game_state_refactor.py
  run chess build

The script preserves the existing WPA2 Enterprise define values from main/server.c, if present.
